{
  "name": "godot-build-service",
  "version": "0.1.0",
  "private": true,
  "description": "Web app that compiles and caches custom Godot engine builds on demand",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "DATA_ROOT=\"${DATA_ROOT:-$PWD/.data}\" concurrently -n api,web -c blue,magenta \"pnpm --filter @godotbuild/api dev\" \"pnpm --filter @godotbuild/web dev\"",
    "build": "pnpm --filter @godotbuild/web build",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r typecheck",
    "start": "DATA_ROOT=\"${DATA_ROOT:-$PWD/.data}\" pnpm --filter @godotbuild/api start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
